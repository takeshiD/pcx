# pcx binary archive

This archive contains the `pcx` command-line tool for Linux.

Run `./pcx --version` to identify the version and `./pcx --help` to get started.
Documentation and source are available at <https://github.com/takeshiD/pcx>.
